Investigate why the following bugs were not caught in the tests (not the actual feedback/feature requests)
Yesterday+Today does not work, shows me the full
Generating full should always land on the current week, both in preview and in the excel file.
The opacity does not work anymore, the ui gets toned down, but the background remains.
Feedback by email should be changed to a link to the issues page.
Timesheet generation from tray does not work or at least does not show the window
Tray add/quick project should just be native gettextinputdialogs, not open the qp. If the user wants to stay in tray mode, he/she should be allowed to do that (the export still uses the preview window, it's just the qp shouldn't be forced on the user or be misused)
Can the about be its own independent window, again as to not rely on QP.
It should not be possible to change the mode of the qp in the tray, only open the qp.
The test for logged time should be longer and more detailed, so the ui/code tests go into edge cases and test on a longer set of data.
