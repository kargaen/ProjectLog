# Project Memory

- New user-facing features and noteworthy fixes should be written to `RELEASE_LOG.md` as they are implemented.
- The release workflow uses `RELEASE_LOG.md` as the release commit message, then clears the file back to its template after a successful release.
